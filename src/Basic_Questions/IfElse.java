package Basic_Questions;

public class IfElse {
    public static void main(String[] args) {
//        int count = 1;       // it will take first as an variable outside
//        while (count != 5) {   // then it will enter the loop and
//            System.out.println(count);  // print the count means all numbers which is not equal to 5
//            count++;              // at last will be increase the count
//        }

//        for (int i = 1; i < 5; i++) {  // for loops make all the conditions and initialize the variable at first so that dont need to go next line
//            System.out.println(i);  // and then it will the execute which is in the loop thatswhy its comfortable and many of they choose "for loop"
//        }


    }
}
